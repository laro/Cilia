func multiply(Matrix A, Matrix B) -> Matrix {
    Int m = A.rows()
    Int n = A.columns()
    Int p = B.columns()

    Matrix C(m, n);

    for i in 0..<m {
        for j in 0..<p {
            for k in 0..<n {
                C[i, j] += A[i, k] * B[k, j]
            }
        }
    }

    return C
}