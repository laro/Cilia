---
permalink: /comparison/csharp/
description: "C# equivalent of Cilia code. List, Matrix, Dictionary, var, const. .NET collections syntax."
---

# Basic Lines of Code in C#

- Types
    - `int`, `long`, `double`
- Variables
    - `int n = 42;`
    - `var n = 42;`
    - `const int n = 42;`
    - `var words = new List<string>();`
    - `var mat = new Matrix<double>(1000, 2000, 0.0);`
    - `var contactInfoForID = new Dictionary<string, ContactInfo>();`
- Functions
    - `static int multiply(int a, int b) { return a * b; }`
    - `static void print(ContactInfo a) { ... }`
    - `static string concat(string a, string b) { return ...; }`
- Loops
    - `for (int i = 1; i <= 10; ++i) { ... }`
    - `for (var i = 0; i < words.Count; ++i) { ... }`
    - `foreach (var i in new[] { 5, 7, 11, 13 }) { ... }`
    - `foreach (var word in words) { ... }`

> **Note**  
> In C# `int` and `42` are 32 bits wide, not 64 bits.  

