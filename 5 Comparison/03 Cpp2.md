---
permalink: /comparison/cpp2/
description: "Cpp2 (Herb Sutter) equivalent of Cilia code. name: Type syntax, n := 42, multiply: (a, b) -> int."
---

# Basic Lines of Code in Cpp2

- Types
    - `int`, `i32`, `i64`, `f64`
- Variables
    - `n: int = 42;`
    - `n := 42;`
    - `n: const int = 42;`
    - `words: vector<string> = ();`
    - `M := matrix<f64>(1000, 2000, 0.0);`
    - `contact_info_for_id: unordered_map<string, contact_info> = ();`
- Functions
    - `multiply: (a: int, b: int) -> int = a * b;`
    - `print: (a: contact_info) = { ...; }`
    - `concat: (a: string_view, b: string_view) -> string = ...;`
- Loops
    - `for 1..=10 do (i) { ...; }`
    - `for 0..<words.size() do (i) { ...; }`
    - `for (5, 7, 11, 13) do (i) { ...; }`
    - `for words do (word) { ...; }`

> **Note**  
> In Cpp2 `int` and `42` are typically 32 bits wide, not 64 bits.  
